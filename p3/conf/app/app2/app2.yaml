apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: app2-deployment
  name: app2-deployment
  namespace: dev
spec:
  replicas: 3
  selector:
    matchLabels:
      app: app2-deployment
  template:
    metadata:
      labels:
        app: app2-deployment
    spec:
      containers:
      - image: icchondocker/vote-app2:v0.3.0
        name: vote-app2
        ports:
        - containerPort: 3000
        env:
        - name: NEXT_PUBLIC_BACKEND_URL
          valueFrom:
            configMapKeyRef:
              name: vote-config
              key: NEXT_PUBLIC_BACKEND_URL
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: app2-deployment
  name: app2-service
  namespace: dev
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: app2-deployment
  type: ClusterIP
